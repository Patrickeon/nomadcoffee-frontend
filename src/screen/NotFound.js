import { HashRouter as Router, Route } from "react-router-dom";

function NotFound() {
  return (
    <div>
      <Router>
        <Route></Route>
      </Router>
    </div>
  );
}

export default NotFound;
